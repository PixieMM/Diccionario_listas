# Definir las listas de claves y valores
claves = ['a', 'b', 'c', 'd']
valores = [1, 2, 3, 4]

# Crear el diccionario utilizando la función zip y el constructor dict
diccionario = dict(zip(claves, valores))

# Imprimir el diccionario resultante
print(diccionario)
